name: Refresh screenshots

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  screenshots:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install evid + shellplot
        run: |
          uv venv .venv --seed
          uv pip install -e .
          # shellplot's setup.py imports pkg_resources without declaring
          # setuptools as a build requirement — pre-install it and disable
          # build isolation so the build can find it. setuptools >=81 dropped
          # the bundled pkg_resources, so pin below that to keep it importable.
          uv pip install "setuptools<81"
          uv pip install --no-build-isolation shellplot

      - name: Regenerate help.svg
        run: |
          source .venv/bin/activate
          shellplot svg single "uv run evid -h 2>/dev/null" \
            -o docs/assets/help.svg --width 120 --font-size 13

      - name: Commit updated screenshots
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: refresh help.svg [skip ci]"
          file_pattern: "docs/assets/help.svg"
